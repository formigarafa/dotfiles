# macos

# start with full thoughtbot/laptop
curl --remote-name https://raw.githubusercontent.com/thoughtbot/laptop/master/mac

# check if you want with:
# less mac

sh mac 2>&1 | tee ~/laptop.log

# dotfiles
git clone https://github.com/formigarafa/dotfiles.git ~/.dotfiles
rcup -v

# running it again, to possibly pick ~/.laptop.local
sh mac 2>&1 | tee ~/laptop.log


# terminal colors
curl --remote-name https://raw.githubusercontent.com/lysyi3m/osx-terminal-themes/master/schemes/3024%20Night.terminal
open 3024%20Night.terminal

# terminal font
curl --remote-name https://raw.githubusercontent.com/ryanoasis/nerd-fonts/master/patched-fonts/SourceCodePro/Regular/complete/Sauce%20Code%20Pro%20Nerd%20Font%20Complete.ttf

# terminal settings: manual
# set default profile
# shell tab: close if exited cleanly
# advanced tab: disable audible bell
# install and select font above (Sauce Code Pro Nerd Font Complete.ttf)
#
#
# go to ttps://pqrs.org/osx/karabiner/seil.html.en download and install Sail
# open and set keycode for Caps Lock to 109
# go to System Preferences --> Keyboard --> Modifier Keys
# set Caps Lock Key to "No Action"


# linux
apt-get install -y git-core curl build-essential
apt-get install -y ruby-dev neovim
apt-get install -y keychain
pip install powerline-shell
curl -LO https://thoughtbot.github.io/rcm/dist/rcm-1.3.0.tar.gz && \
\
sha=$(sha256sum rcm-1.3.0.tar.gz | cut -f1 -d' ') && \
[ "$sha" = "ddcf638b367b0361d8e063c29fd573dbe1712d1b83e8d5b3a868e4aa45ffc847" ] && \
\
tar -xvf rcm-1.3.0.tar.gz && \
cd rcm-1.3.0 && \
\
./configure && \
make && \
sudo make install

# all
git clone git@github.com:formigarafa/dotfiles.git ~/.dotfiles
rcup -v
git clone https://github.com/VundleVim/Vundle.vim.git ~/.vim/bundle/Vundle.vim
vim +PluginInstall +qall
